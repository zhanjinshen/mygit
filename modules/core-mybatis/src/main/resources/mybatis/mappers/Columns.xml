<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="columns">

    <sql id="fileMetadata">
        FFM.ID          AS FFM_ID,
        FFM.SUFFIX      AS FFM_SUFFIX,
        FFM.NAME        AS FFM_NAME,
        FFM.SIZE        AS FFM_SIZE,
        FFM.CREATED_AT  AS FFM_CREATED_AT
    </sql>

</mapper>