<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="columns">

    <sql id="fileMetadata">
        FFM.ID          AS FFM_ID,
        FFM.SOURCE_NAME AS FFM_SOURCE_NAME,
        FFM.DEST_NAME   AS FFM_DEST_NAME,
        FFM.SUFFIX      AS FFM_SUFFIX,
        FFM.SIZE        AS FFM_SIZE,
        FFM.CREATED_AT  AS FFM_CREATED_AT,
        FFM.SWF_FILE_NAME  AS FFM_SWF_FILE_NAME
    </sql>

</mapper>